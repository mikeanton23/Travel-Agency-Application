# Shell scripts must keep LF endings even when checked out on Windows,
# otherwise the container entrypoint fails with "bad interpreter".
*.sh text eol=lf
Dockerfile text eol=lf
*.yaml text eol=lf
*.yml text eol=lf
